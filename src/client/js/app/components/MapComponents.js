/** @jsx React.DOM */

define([
    'react'
], function(
    React
) {
    'use strict';

    var MapComponents = React.createClass({

        getInitialState: function() {
            return {

            }
        }

        , render: function(){
            return (
                <div>
                </div>
            );
        }
    });

    return MapComponents;

});
