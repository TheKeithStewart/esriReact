/** @jsx React.DOM */

define([
    'react'
    , 'app/config'
], function(
    React
    , config
) {

    var Header = React.createClass({

        render: function(){
            return (
                <div>
                    Header
                </div>
            );
        }

    });

    return Header;
});
