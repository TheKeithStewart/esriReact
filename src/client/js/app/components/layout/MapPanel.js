/** @jsx React.DOM */

define([
    'react'
], function(React) {
    'use strict';

    var MapPanel = React.createClass({
        render: function(){
            return (
                <div>
                    Map Panel
                </div>
            );
        }
    });

    return MapPanel;
});
